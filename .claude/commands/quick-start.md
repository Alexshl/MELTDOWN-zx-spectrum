---
description: Onboard a new ZX Spectrum project on top of zx-framework — runs a discovery interview, drafts docs/PROJECT.md, generates the first 3–7 tasks in docs/tasks/, and verifies the Docker toolchain builds and passes smoke.
---

Invoke the `quick-start` skill and follow its workflow end to end.
