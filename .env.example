# Override these to match your host user (Linux/macOS). Ignored on Windows.
UID=1000
GID=1000

# Tag for the framework image
IMAGE_TAG=zx-framework:latest

# Trace / investigate runtime parameters (uncomment and set as needed)
# BIN=build/app_CODE.bin
# CYCLES=2000000
# FILE=imports/sample.tap
# Q="what is the main loop structure"
# ORG=0x6000
