# NN. <Title>

**Status**: TODO
**Depends on**: <id or —>
**Blocks**: <id or —>

## Goal

<one sentence describing what must work when this task is done>

## Context

<why the task is needed, what it builds on, which architectural decisions it touches>

## Acceptance criteria

- [ ] ...
- [ ] ...

## Test plan

<!-- Pick ONE mode below and delete the other variants. -->
<!-- Without this section the tester returns FAIL with test-plan-mismatch. -->

### Variant A — skip (task has no runtime behaviour to verify)

```
skip: <reason, e.g. "documentation only" or "infrastructure only">
```

### Variant B — smoke-only (a single `make smoke` run is sufficient)

```
smoke-only: <expectation, e.g. "build succeeds, smoke.scr=6912 bytes, PC≠0x0000">
```

### Variant C — scenarios (run specific integration scenarios)

```
scenarios: [scenario-a, scenario-b]
```

## Out of scope

- <features or ideas not in this task's scope, but worth tracking for later>
